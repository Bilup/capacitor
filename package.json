{
  "name": "bilup-capacitor",
  "version": "1.6.0",
  "description": "Bilup Desktop Capacitor Version",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bilup/capacitor.git"
  },
  "author": {
    "email": "support@bilup.org",
    "name": "Bilup"
  },
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/Bilup/capacitor/issues"
  },
  "homepage": "https://github.com/Bilup/capacitor#readme",
  "private": true,
  "scripts": {
    "prebuild": "npm run fetch:gui && npm run build:gui && npm run cap:copy",
    "fetch:gui": "if [ -d scratch-gui ]; then cd scratch-gui && git pull origin develop; else git clone -b develop --single-branch https://github.com/Bilup/scratch-gui.git; fi",
    "build:gui": "cd scratch-gui && npm ci && npm run build",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1"
  }
}
